import { match } from "./matches"
import { schoolOnePlayer, schoolTwoPlayer } from "./players"

export const board = {
    id: "clry174rs000a08jo0xoohaqg",
    matchId: match.id,
    homePlayerId: schoolOnePlayer.id,
    awayPlayerId: schoolTwoPlayer.id,
    number: 7
}
